import 'package:flutter/material.dart';

SizedBox getSizedBoxForHeight({required double height}) => SizedBox(
      height: height,
    );

SizedBox getSizedBoxForWidth({required double width}) => SizedBox(
      width: width,
    );
